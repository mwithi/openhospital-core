<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0
                             https://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <groupId>org.isf</groupId>
  <artifactId>OH-plugin-spi-test</artifactId>
  <version>1.0.0</version>
  <packaging>jar</packaging>

  <name>OH Plugin SPI — Test Utilities</name>
  <description>
    In-memory stub implementations of all OH-plugin-spi interfaces.
    Add this artefact with scope "test" to test your plugin without
    Spring, a database, or a running OH instance.
  </description>
  <url>https://www.open-hospital.org</url>

  <licenses>
    <license>
      <name>GNU General Public License v3.0</name>
      <url>https://www.gnu.org/licenses/gpl-3.0-standalone.html</url>
    </license>
  </licenses>

  <organization>
    <name>Informatici Senza Frontiere</name>
    <url>https://www.informaticisenzafrontiere.org</url>
  </organization>

  <properties>
    <java.version>17</java.version>
    <maven.compiler.source>17</maven.compiler.source>
    <maven.compiler.target>17</maven.compiler.target>
    <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
    <spi.version>1.0.0</spi.version>
  </properties>

  <dependencies>

    <!-- The SPI this module provides stubs for.
         Marked compile so plugin developers get it transitively
         with a single test-scoped dependency. -->
    <dependency>
      <groupId>org.isf</groupId>
      <artifactId>OH-plugin-spi</artifactId>
      <version>${spi.version}</version>
    </dependency>

    <!-- SLF4J simple binding — gives tests a real logger without Logback -->
    <dependency>
      <groupId>org.slf4j</groupId>
      <artifactId>slf4j-simple</artifactId>
      <version>2.0.13</version>
    </dependency>

    <!-- Test dependencies for running tests inside plugin-spi-test itself -->
    <dependency>
      <groupId>org.junit.jupiter</groupId>
      <artifactId>junit-jupiter</artifactId>
      <version>5.11.3</version>
      <scope>test</scope>
    </dependency>
    <dependency>
      <groupId>org.assertj</groupId>
      <artifactId>assertj-core</artifactId>
      <version>3.26.3</version>
      <scope>test</scope>
    </dependency>

  </dependencies>

  <build>
    <plugins>

      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-compiler-plugin</artifactId>
        <version>3.13.0</version>
        <configuration>
          <release>17</release>
          <compilerArgs>
            <arg>-Xlint:all</arg>
            <arg>-Xlint:-processing</arg>
          </compilerArgs>
        </configuration>
      </plugin>

      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-surefire-plugin</artifactId>
        <version>3.5.1</version>
      </plugin>

      <!-- Produce a sources JAR so IDEs can navigate into stub code -->
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-source-plugin</artifactId>
        <version>3.3.1</version>
        <executions>
          <execution>
            <id>attach-sources</id>
            <goals><goal>jar-no-fork</goal></goals>
          </execution>
        </executions>
      </plugin>

    </plugins>
  </build>

</project>
